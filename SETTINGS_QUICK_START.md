# 🚀 نظام الإعدادات - بدء سريع

## ما الذي تم إضافته؟

### ✅ 4 ملفات جديدة:

1. **Model** - `app/Models/SiteSetting.php`
   - لتخزين واسترجاع الإعدادات

2. **Migration** - `database/migrations/2026_01_28_000000_create_site_settings_table.php`
   - لإنشاء جدول الإعدادات

3. **Seeder** - `database/seeders/SiteSettingsSeeder.php`
   - لإضافة الإعدادات الافتراضية

4. **Helper** - `app/Support/SettingsHelper.php`
   - للوصول السهل للإعدادات

5. **Controller** - `app/Http/Controllers/Admin/SettingsController.php`
   - لتعديل الإعدادات من الـ Dashboard

---

## 🎯 الإعدادات المتاحة حالياً:

### 🎨 **الألوان:**
- اللون الأساسي (Primary)
- اللون الثانوي (Secondary)
- اللون المميز (Accent)
- اللون الداكن (Dark)

### 📱 **وسائل التواصل:**
- Facebook, Twitter, Instagram, LinkedIn
- YouTube, WhatsApp

### 🏢 **معلومات المؤسسة:**
- الاسم، البريد، الهاتف، العنوان
- الوصف

### 📝 **المحتوى:**
- عنوان القسم الرئيسي
- وصف القسم الرئيسي
- وصف الفوتر

---

## 🔨 خطوات التشغيل:

### 1. تشغيل Migration:
```bash
php artisan migrate
```

### 2. تشغيل Seeder:
```bash
php artisan db:seed --class=SiteSettingsSeeder
```

---

## 📖 أمثلة الاستخدام:

### في الـ View:
```blade
<!-- الحصول على إعداد واحد -->
{{ SettingsHelper::get('organization_name') }}

<!-- الحصول على اللون الأساسي -->
<div style="background: {{ SettingsHelper::getColors()['primary'] }}">
    ...
</div>

<!-- روابط التواصل الاجتماعي -->
@foreach(SettingsHelper::getSocialLinks() as $key => $link)
    <a href="{{ $link }}">{{ ucfirst($key) }}</a>
@endforeach
```

### في الـ Controller:
```php
use App\Support\SettingsHelper;

$colors = SettingsHelper::getColors();
$org = SettingsHelper::getOrganization();
```

---

## 🎛️ الإعدادات الافتراضية:

| الإعداد | القيمة الافتراضية |
|-------|-----------------|
| اللون الأساسي | `#1779BA` |
| اللون الثانوي | `#198754` |
| اللون المميز | `#FF6B35` |
| اللون الداكن | `#1B2631` |
| اسم المؤسسة | جمعية السلام |
| البريد | info@al-salam.org |
| الهاتف | +966501234567 |

---

## ✨ المميزات:

✅ تغيير الألوان من الـ Dashboard
✅ تغيير روابط التواصل الاجتماعي
✅ تغيير معلومات المؤسسة
✅ تغيير النصوص الثابتة
✅ بدون الحاجة للعودة للكود
✅ جميع البيانات محفوظة في قاعدة البيانات

---

## 🚀 الخطوة التالية:

تحديث الـ Views لاستخدام الإعدادات بدل النصوص الثابتة:

```blade
<!-- قبل -->
<h1>جمعية السلام</h1>

<!-- بعد -->
<h1>{{ SettingsHelper::get('organization_name') }}</h1>
```

---

**جاهز للاستخدام! 🎉**
